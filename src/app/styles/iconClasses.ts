import style9 from "style9";

export const { ...icon } = style9.create({
  iconContainer: {
    minWidth: 56,
    minHeight: 32,
    justifyContent: "center",
    alignItems: "flex-start",
    alignSelf: "stretch",
  },
});
